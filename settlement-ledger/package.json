{
  "name": "settlement-ledger",
  "version": "1.0.0",
  "private": true,
  "description": "Hardhat workspace for SettlementVault local final settlement",
  "scripts": {
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "compile": "hardhat compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "ethers": "^6.13.4",
    "hardhat": "^2.22.18"
  }
}
